global class YourekaSchedulerApex implements Schedulable {
global void execute(SchedulableContext ctx) {
    //YourekaMobileCheckBoxUncheck batch = new YourekaMobileCheckBoxUncheck();
    //database.executebatch(batch); //comment out for IBA-4194
     }
}